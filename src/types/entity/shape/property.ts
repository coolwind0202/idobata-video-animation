type _Color = {
    r: number,
    g: number,
    b: number
};

type FillColor = _Color;

type LineColor = _Color;

type LineWeight = {
    value: number;
};

export type { FillColor, LineColor, LineWeight };