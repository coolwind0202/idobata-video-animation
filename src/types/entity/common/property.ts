type Position = {
    x: number,
    y: number,
};

type Scale = {
    xScale: number;
    yScale: number;
};

type Transparency = {
    value: number;
};

type Rotation = {
    angle: number
};

export type { Position, Scale, Transparency, Rotation };